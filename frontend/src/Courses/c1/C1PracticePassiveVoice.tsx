import Quiz from '../Quiz'
import { passiveVoiceData } from '../../data/data'

const C1PracticePassiveVoice = () => {
  return <Quiz data={passiveVoiceData}></Quiz>
}

export default C1PracticePassiveVoice
